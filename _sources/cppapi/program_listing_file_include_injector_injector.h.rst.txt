
.. _program_listing_file_include_injector_injector.h:

Program Listing for File injector.h
===================================

|exhale_lsh| :ref:`Return to documentation for file <file_include_injector_injector.h>` (``include/injector/injector.h``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   /******************************************************************************
   © Intel Corporation.
   
   This software and the related documents are Intel copyrighted materials,
   and your use of them is governed by the express license under which they
   were provided to you ("License"). Unless the License provides otherwise,
   you may not use, modify, copy, publish, distribute, disclose or transmit
   this software or the related documents without Intel's prior written
   permission.
   
   
    This software and the related documents are provided as is, with no express
   or implied warranties, other than those that are expressly stated in the
   License.
   
   ******************************************************************************/
   
   #pragma once
   
   #include "logger/logger.h"
   #include "utility/common.h"
   #include <stdio.h>
   #include <cstdint>
   #include <string>
   
   namespace gpa {
   
   enum class InjectionResult {
       kSuccess,
       kInvalidParameter,  
       kTargetNotFound,    
       kInvalidDirectory,  
       kInjectionFailed,   
       kUnsupported,       
   };
   
   constexpr char kSharedMemoryEnvVar[] = "GPA_SHARED_MEMORY";
   
   using namespace gpa::utility;
   struct InjectionProperties
   {
       char logLevel[kLogLevelLength]{};                              
       TCHAR ipcName[kMaxPathLength]{};                               
       TCHAR logFilename[kLogFilenameLength]{};                       
       TCHAR layerNames[kMaxLayers][kLayerNameLength]{};              
       uint64_t layerKeyValueCount[kMaxLayerArgs]{};                  
       uint32_t numLayers = 0;                                        
       uint32_t enableKeyframing = 0;                                 
       uint32_t pageTrackerMode = 0;                                  
       gpa::logger::Options logOptions;                               
       volatile uint32_t processCounter = 0;                          
       volatile int32_t pids[kMaxProcesses]{};                        
       KeyValPair keyValuePairsForLayers[kMaxLayers][kMaxLayerArgs];  
       TCHAR waitForDebugger[kLogFilenameLength]{};                   
       uint32_t disableKeyboardEvents = 1;                            
       uint32_t hookApiFlags = HookApiFlagBits::kHookAll;             
       uint32_t hookD3D11On12 = 0;                                    
       uint32_t injectionMode = InjectionMode::kSetThreadContext;
   };
   
   InjectionResult InjectSuspendedApplication(void* process, void* thread, uint32_t injectionMode);
   
   InjectionResult LaunchAndInjectApplication(char const* application, char const* workingDirectory,
                                              int argc, char const* const* argv,
                                              InjectionProperties const& injectionProperties = {});
   
   std::string GetSharedMemoryName(int32_t pid);
   
   InjectionProperties* GetInjectionProperties();
   
   }  // namespace gpa
